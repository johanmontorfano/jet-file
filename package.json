{
    "name": "jet-file",
    "version": "2.0.0",
    "description": "An efficient, production-ready, encrypted database utility based on a key system. ",
    "main": "./dist/index.js",
    "scripts": {
        "test": "ts-node ./tests/io.ts",
        "build": "tsc",
        "commit": "git add .; git commit -am '2.0.0'; git push; git fetch"
    },
    "dependencies": {
        "fs-extra": "^10.1.0"
    },
    "devDependencies": {
        "@types/fs-extra": "^9.0.13"
    }
}
