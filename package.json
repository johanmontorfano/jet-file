{
    "name": "jet-file",
    "version": "2.0.8",
    "description": "An efficient, production-ready, encrypted database utility.",
    "main": "./dist/index.js",
    "scripts": {
        "test": "ts-node ./tests/io.ts",
        "build": "tsc",
        "commit": "git add .; git commit -am '2.0.2'; git push; git fetch"
    },
    "dependencies": {
        "fs-extra": "^10.1.0"
    },
    "devDependencies": {
        "@types/fs-extra": "^9.0.13"
    }
}
